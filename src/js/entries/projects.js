// vue JS
import Vue from 'vue';

// UIkit Common dependencies
require('../uikit-common.js');

window.onload = function () {
    new Vue({
        el: '#app-projects',
        data: {
            projects: 'tesst'
        }
    });
};
